-- pi.dev AI coding agent, toggled inside a terminal split.
-- Requires the `pi` CLI in PATH (https://pi.dev). Replaces the old claudecode.nvim.
return {
  {
    "aldoborrero/pi-agent.nvim",
    opts = {},
  },
}
